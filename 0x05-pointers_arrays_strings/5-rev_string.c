#include <stdio.h>
#include <string.h>
/**
 * rev_string - check code
 * @s: sring
 * Return: revers
 */
void rev_string(char *s)
{
	int len = strlen(s);
	int i = len - 1, j = 0;
	char st[] = s[];
	while (i >= 0)
	{
		s[j] = st[i];
		j++;
		i--;
	}

}
