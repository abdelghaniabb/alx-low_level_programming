#include "main.h"
#include <stdio.h>
/**
 * print_sign - chek code
 * @n: integer
 * Return: 1 or 0
 */
int print_sign(int n)
{
	if (n > 0)
	{
                return (1);
		printf("%c", '+');
	}
	else if (n == 0)
	{
                return (1);
		printf("%c", '0');
	}
	else
	{
                return (1);
		printf("%c", '-');
	}
}
